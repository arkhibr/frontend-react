// src/shared/lib/validators/__tests__/smoke.test.ts
import { describe, it, expect } from 'vitest'

describe('vitest setup', () => {
  it('está configurado corretamente', () => {
    expect(1 + 1).toBe(2)
  })
})
